export const metadata = {
  title: "Prompta Proxy",
  description: "OpenRouter API Proxy for Prompta"
};

export default function RootLayout({ children }) {
  return (
    <html lang="zh-Hant">
      <body>{children}</body>
    </html>
  );
}
